# dining_hall_food_ranker

# Local Dev Setup

1. Rename `frontend/.env.example` -> `frontend/.env`. Change the value to your local api base url (should be http://localhost:8000 by default)
2. Switch out prod lines for local dev related lines in `frontend/Dockerfile`
