info:
  name: Get Two Random Dishes
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8000/meals/random
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
